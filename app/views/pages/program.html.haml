%h2
  Programmes

%ul.programs
  - @sections.each do |section|
    %li= link_to section.title, "/pages/program?r=#{section.title}"

- @programs.each do |program|
  .program
    %h4
      = (program.title.gsub("\n", "<br />")).html_safe
    %p
      - if program.image_url.present?
        = image_tag(program.image_url(:thumb))
      = (program.description.gsub("\n", "<br />")).html_safe

%h2.repertoire_title
  Répertoire

%table.repertoires
  - @repertoires.each_slice(2) do |repertoires|
    %tr
      - repertoires.each do |repertoire|
        %td
          .repertoire
            %h3
              = (repertoire.title.gsub("\n", "<br />")).html_safe
            %p.program
              = (repertoire.description.gsub("\n", "<br />")).html_safe

%p.link-to-top
  = link_to "retour en haut de page", "#"